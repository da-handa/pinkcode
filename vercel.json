{
  "version": 2,
  "builds": [
    {
      "src": "pinkcodeserver/target/release/pinkcodeserver", // 수정된 폴더명 적용
      "use": "@vercel/node"
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/pinkcodeserver/target/release/pinkcodeserver" // 수정된 폴더명 적용
    }
  ]
}
